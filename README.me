1 - Para rodar o backend precisa instalar o pip com o comando:

curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

2 - E depois:

python3 get-pip.py

3 - Após isso poderá instalar o flask:

pip install flask
pip install flask_cors

4 - Finalmente poderá rodar o servidor

python3 server.py